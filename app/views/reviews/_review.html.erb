<p><%= review.rating %></p>
<p><%= review.review %></p>

  <% if user_signed_in? %>

  <%= link_to "Edit", edit_book_review_path(review.book, review) %>
  <%= link_to "Delete", book_review_path(review.book, review), method: :delete, data: { confirm: "Didnt like this review?" } %>

<% end %>