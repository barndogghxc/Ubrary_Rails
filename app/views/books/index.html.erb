<h1 class="current-genre"><%= params[:genre]%> Books</h1>


<% @books.each do |book| %>
  <a href="/books/<%= book.id %>">
  	<%= image_tag book.book_img.url(:book_index) %>
  </a>
<% end %>

<%= link_to "Add Book", new_book_path %>